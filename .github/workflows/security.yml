name: Security Scan (reusable)
on:
  workflow_call:
    inputs:
      docker-images:
        description: 'JSON array'
        type: string
        default: '[]'
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "docker=${{ inputs.docker-images }}"
